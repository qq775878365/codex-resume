# 简历网页项目工作记录

**日期**：2026年6月4日  
**项目**：张梓宇 - AI自动化工作流工程师 网页简历  
**仓库**：https://github.com/qq775878365/codex-resume  
**线上地址**：https://qq775878365.github.io/codex-resume/

---

## 一、项目概述

为张梓宇创建了一个现代化的网页简历，展示其作为 AI 自动化工作流工程师的技能、项目经验和作品集。整个网页由 Codex CLI 协助完成，使用纯 HTML/CSS 构建，无框架依赖。

---

## 二、工作流程

### 2.1 项目初始化
- 发现 `D:\Codex1\ceshi\codex-resume\index.html` 已有简历网页文件
- 该文件由 Codex CLI 生成，包含完整的暗色主题简历

### 2.2 GitHub 仓库创建
- 使用 GitHub Personal Access Token 认证（账号：qq775878365）
- 通过 GitHub REST API 创建公开仓库 `codex-resume`
- 需要的 Token 权限：Administration（创建仓库）、Contents（读写代码）、Pages（开启 Pages）
- 初始 token 权限不足，经过多次调整权限后成功

### 2.3 Git 初始化与首次推送
- `git init` 初始化本地仓库
- 移除 `codex-resume/` 目录内嵌的 `.git`（避免 submodule 问题）
- 将 `index.html` 移至仓库根目录（优化 GitHub Pages 访问路径）
- 首次推送成功

### 2.4 GitHub Pages 开启
- 通过 GitHub API 开启 Pages：`POST /repos/{owner}/{repo}/pages`
- 部署分支：main，路径：/ (root)
- 访问地址：`https://qq775878365.github.io/codex-resume/`

---

## 三、设计修改

### 3.1 背景主题：暗色 → 白色
**修改的 CSS 变量：**
| 变量 | 原值（暗色） | 新值（白色） |
|------|------------|------------|
| `--bg-primary` | `#0a0a0f` | `#ffffff` |
| `--bg-card` | `#12121a` | `#f4f5f7` |
| `--bg-card-hover` | `#1a1a2e` | `#ebedf0` |
| `--text-primary` | `#e8e8f0` | `#1a1a2e` |
| `--text-secondary` | `#8888aa` | `#666680` |
| `--text-dim` | `#555577` | `#9999aa` |
| `--border` | `rgba(255,255,255,0.06)` | `rgba(0,0,0,0.08)` |

**其他调整：**
- 导航栏背景：`rgba(10,10,15,0.8)` → `rgba(255,255,255,0.92)`
- Section 交替背景：`rgba(255,255,255,0.01)` → `rgba(0,0,0,0.02)`
- 动画背景渐变透明度调低

### 3.2 字体放大
- 主标题（Hero h1）：`clamp(36px, 6vw, 64px)` → `clamp(42px, 7vw, 76px)`
- 板块标题（Section h2）：`32px` → `36px` → `44px`

### 3.3 AI 生图提示
- 在「团购宣传海报」上方添加醒目标识
- 样式：紫色渐变背景 + 紫色边框 + 16px 加粗字体
- 内容：`✨ 以下所有图片均由 AI 生成（gpt-image-2）`

### 3.4 导航栏 AI 声明
- 在「张梓宇」名字后添加说明文字
- 内容：`本网页由 AI 部署，所有内容均由 AI 生成（真实数据除外）`
- 样式：11px 灰色小字

### 3.5 策略文档卡片链接颜色
- 「点击查看详情 →」文字改为紫色（`#6c63ff`）加粗
- 增强可点击感

---

## 四、作品展示板块（Portfolio）

### 4.1 添加导航链接
- 导航栏新增「作品展示」快捷入口，链接到 `#portfolio`

### 4.2 工作流概览统计
| 数据 | 数值 |
|------|------|
| 自动化工作流 | 3 |
| AI 设计作品 | 16 |
| 调研报告字数 | 15000+ |
| 每日情报产出 | 3 |

### 4.3 策略文档（可点击查看详情）
| 文档 | 链接 | 说明 |
|------|------|------|
| AI工作流设计文档 | `docs/workflow.html` | 3条自动化流水线 + Prompt模板 + 效率数据 |
| 团购行业市场调研报告 | `docs/market-research.html` | 15000+字行业全景分析 |
| 团购行业每日情报 | `docs/daily-0601.html` | AI自动生成的行业简报 |

### 4.4 团购宣传海报（3张）
| 海报 | 比例 | 说明 |
|------|------|------|
| 火锅团购海报 | 9:16 竖版 | 中国红主色调，手机端适配 |
| 美容美发团购海报 | 3:2 横版 | 粉色系高端感，电脑端Banner |
| 生鲜水果团购海报 | 1:1 方形 | 清新绿色，多平台通用 |

### 4.5 产品主图（9张）
- **餐饮组**：麻辣牛肉、鲜毛肚、菌菇拼盘（白底美食摄影）
- **生鲜组**：橙子、草莓、蓝莓（白底水果摄影）
- **美容组**：化妆服务、美发造型、面部护理（高端人像）

### 4.6 营销物料（4张）
- 美食节活动 Banner
- 美容美发活动 Banner
- 折扣券设计
- 满减券设计

### 4.7 PPT 方案（在线预览）
| PPT | 大小 | 说明 |
|-----|------|------|
| AI赋能团购品牌营销方案 | 38.8MB | 完整营销策划方案，使用微软 Office Online 嵌入预览 |
| 图片转PPT演示 | 16MB | AI办公自动化能力展示 |

### 4.8 AI 视频作品（内嵌播放器）
| 视频 | 大小 | 说明 |
|------|------|------|
| 介绍水杯 | 2.2MB | 产品展示视频 |
| 介绍耳机 | 2.7MB | 产品展示视频 |
| 轻松舞蹈 | 9.1MB | AI创意视频 |

---

## 五、文档页面

### 5.1 页面列表
| 页面 | 文件 | 内容 |
|------|------|------|
| AI工作流设计文档 | `docs/workflow.html` | 3条自动化流水线详细设计、Prompt模板、效率对比数据 |
| 团购行业市场调研报告 | `docs/market-research.html` | 15000+字行业全景分析，含市场规模、竞争格局、用户画像 |
| 每日情报 6月1日 | `docs/daily-0601.html` | AI自动生成的行业动态简报 |
| 每日情报 6月3日 | `docs/daily-0603.html` | AI自动生成的行业动态简报 |

### 5.2 页面样式
- 白色背景 + 紫色主题色（`#6c63ff`）
- 顶部「← 返回简历」导航
- 完整的 Markdown 渲染样式（表格、代码块、引用）
- 移动端自适应

### 5.3 技术实现
- 使用 Python `markdown` 库将 `.md` 文件转为 HTML
- 共享 HTML 模板，统一视觉风格
- 支持 `tables`、`fenced_code`、`toc` 扩展

---

## 六、图片处理

### 6.1 原始文件
- 来源目录：`D:\workbuddy-cw\图片\`
- 原始大小：每个 4-7MB（总计约 70MB）
- 格式：PNG

### 6.2 压缩处理
- 使用 Python PIL/Pillow 库
- 缩放至最长边 800px
- 保持 PNG 格式，启用 optimize
- 压缩后大小：每个 377KB-1020KB（总计约 10MB）
- 压缩率：约 85%

### 6.3 目录结构
```
portfolio/
├── posters/          # 团购海报（3张）
├── products/         # 产品主图（9张）
├── materials/        # 营销物料（4张）
├── *.pptx            # PPT文件（2个）
└── *.mp4             # 视频文件（3个）
```

---

## 七、技术细节

### 7.1 仓库信息
- **GitHub 账号**：qq775878365
- **仓库名**：codex-resume
- **仓库类型**：Public
- **默认分支**：main
- **GitHub Pages**：已开启，源分支 main

### 7.2 Token 权限要求
| 权限 | 用途 |
|------|------|
| Administration（Read & Write） | 创建仓库 |
| Contents（Read & Write） | 推送代码 |
| Pages（Read & Write） | 开启 GitHub Pages |

### 7.3 文件大小
| 类型 | 数量 | 总大小 |
|------|------|--------|
| HTML 页面 | 5 | ~200KB |
| 图片（压缩后） | 16 | ~10MB |
| PPT 文件 | 2 | ~55MB |
| 视频文件 | 3 | ~14MB |
| **总计** | - | **~80MB** |

### 7.4 环境依赖
- **Git**：版本控制
- **GitHub CLI (gh)**：GitHub 操作
- **Python PIL/Pillow**：图片压缩
- **Python markdown**：Markdown 转 HTML
- **代理工具**：端口 10808（SOCKS5），访问 GitHub 需要开启

---

## 八、遇到的问题及解决方案

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| Git 添加 `codex-resume` 时提示 embedded repository | 目录内有 `.git` | `git rm --cached` 后删除内嵌 `.git` |
| GitHub Token 无法创建仓库 | Fine-grained token 缺少 Administration 权限 | 在 token 设置中添加 Administration 读写权限 |
| GitHub Token 无法推送代码 | 缺少 Contents 写权限 | 添加 Contents 读写权限 |
| GitHub Token 无法开启 Pages | 缺少 Pages 权限 | 添加 Pages 读写权限 |
| `gh auth login` 超时 | 沙箱网络限制 + 需要浏览器交互 | 改用 Token 方式登录 |
| `curl` 命令报错 | PowerShell 将 `curl` 映射为 `Invoke-WebRequest` | 使用 `Invoke-RestMethod` |
| `Start-Process` 打开文件超时 | 沙箱限制 | 改用 HTTP 服务器 + 浏览器访问 |
| Git push 超时 | 代理工具未运行 | 用户开启代理后重试 |
| HTML 文件格式被破坏 | `Set-Content -NoNewline` + 数组拼接 | 使用 `[System.IO.File]::WriteAllLines()` |
| 策略文档卡片布局错乱 | 多余的 `</div>` 标签 | 手动定位并删除重复标签 |
| 策略文档卡片重复 | 替换操作引入重复内容 | 精确删除重复行 |

---

## 九、Git 提交历史

| 提交 | 说明 |
|------|------|
| `0143bc6` | Move index.html to root for GitHub Pages |
| `cb7b2a6` | Change background to white theme |
| `763c2bd` | Add portfolio section with AI-generated marketing works |
| `2704b3e` | Add PPT previews and AI video works |
| `e0e86fe` | Add document pages with full content viewer |
| `98a623c` | Make hero title and section headers larger |
| `e19807f` | Make section headers bigger (44px) |
| `ad834ed` | Add AI-generated note above posters |
| `ea07b26` | Make AI-generated badge more prominent |
| `6f62413` | Fix strategy docs grid alignment (3 columns) |
| `a7b376e` | Remove duplicate doc cards |
| `c3a7936` | Add AI deployment note in nav bar |
| `e4079d7` | Add accent color to 'click to view' links |

---

## 十、后续可优化

- [ ] 添加深色/浅色主题切换
- [ ] 添加简历 PDF 下载按钮
- [ ] 优化移动端适配
- [ ] 添加页面加载动画
- [ ] 考虑使用 GitHub Actions 自动部署
- [ ] 添加更多互动元素（如技能进度条动画）

---

*记录人：Codex CLI*  
*最后更新：2026-06-04*
